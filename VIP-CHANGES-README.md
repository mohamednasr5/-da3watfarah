# تحديثات قسم VIP — ملخص التغييرات

## الملفات الجديدة

- **`vip/index.html`** — الصفحة الرئيسية لباقة VIP على `da3watfarah.com/vip/`
  - بطاقة الاشتراك (VIP – 400 ج.م مدى الحياة) في الأعلى
  - تابات الأقسام: الكل / زفاف / خطوبة / حناء / عيد ميلاد / مولود / تخرج
  - شبكة بها كل الـ 17 تصميم، كل تصميم فيه:
    - فيديو تلقائي عند مرور الماوس (أو أيقونة بديلة للتصاميم بدون فيديو: `editorial-noir` و`royal-maroon`)
    - علامة "VIP حصري"
    - زر **معاينة** (يفتح صفحة الديمو الجاهزة)
    - زر **استخدام الآن** (يوديك لصفحة `create-invitation.html` مع تفعيل باقة VIP تلقائيًا)

- **`vip/assets/vip.css`** و **`vip/assets/vip-widget.js`** — ملفات مشتركة تُحقن في كل صفحات الديمو الـ 17:
  - شريط "تصميم حصري VIP" أعلى يسار الصفحة
  - كارت عائم أسفل الصفحة "أعجبتك هذه الدعوة؟" فيه السعر وزر "اشترِ وخصصها الآن" + رابط واتساب

## الملفات المعدَّلة

- **17 صفحة ديمو** داخل `vip/<القسم>/<اسم القالب>/*.html` — تمت إضافة سطرين فقط قبل `</body>` لربط الـ widget (لم يتم لمس أي كود آخر بداخلها، ولم تُغيَّر الفيديوهات).

- **`create-invitation.html`**:
  1. إضافة تصنيف `badge-vip` وعرضه على أي قالب فيه `isVip: true` (نفس أسلوب `isPremium` الموجود).
  2. عند وصول الزائر برابط فيه `?vip=1&source=اسم_التصميم` (وده اللي زر "استخدام الآن" بيعمله)، بيتم:
     - حفظ اختيار باقة VIP تلقائيًا (باستخدام آلية `da3wa_selected_plan` الموجودة أصلاً في المشروع)
     - إظهار بانر واضح أعلى الصفحة يقول إن هذا التصميم حصري لمشتركي VIP، وسعره (400 ج.م / 500 ر.س / $25)
     - نفس البانر يظهر تاني في شاشة اختيار الباقة وشاشة الدفع، عشان العميل ميضيعش السياق ويشوف زر "ادفع الآن" بوضوح

## ملاحظة مهمة عن حدود التنفيذ

تصاميم الـ VIP الـ 17 اللي موجودة في مجلد `vip/` هي صفحات دعوة **جاهزة وكاملة** (فيها اسم عريس وعروسة وتاريخ ثابت داخل الكود)، مش قوالب ديناميكية زي اللي في `TEMPLATES_CONFIG` بداخل `create-invitation.html` (اللي بتتغير حسب بيانات كل عميل). يعني دلوقتي:

- أي حد يزور `/vip/` هيقدر **يتصفح ويعاين** كل تصميم، ولما يضغط "استخدام الآن" هيتوجه لصفحة إنشاء الدعوة مع تفعيل شرط VIP والسعر تلقائيًا — بالضبط زي ما طلبت.
- لكن التصميم نفسه (الألوان/الخطوط/التنسيق) لسه مش متاح كخيار فعلي داخل `TEMPLATES_CONFIG` عشان العميل يقدر "يستخدمه فعليًا" ببياناته هو. ده شغل إضافي (تحويل كل تصميم من الـ 17 لقالب ديناميكي زي باقي القوالب) — لو عايز أكمله في الخطوة الجاية قولي وهبدأ بيه.

## كيفية الدمج

انسخ الملفات دي فوق نفس المسارات في مشروعك الحقيقي (Git repo)، وراجع الـ diff بتاع `create-invitation.html` قبل الـ commit.

---

## تحديث إضافي: إصلاح حروف المونوجرام الثابتة (م و م / ع و ن)

في 3 قوالب زفاف VIP كانت حروف المونوجرام الزخرفية (على الظرف/الشعار) مكتوبة كنص ثابت (hardcoded) في الـ HTML مباشرة، فتظهر دائمًا بنفس الحروف بغض النظر عن اسم العريس والعروس الفعليين:

- **`demo-editorial-noir`** و **`demo-garden-blush`**: عنصر `.env-letter-monogram` على غلاف الظرف كان يعرض "م و م" ثابتًا.
- **`demo-royal-maroon`**: عنصرين — شعار التنقّل `.nav-monogram` وختم الظرف `.envw-letters` — كانا يعرضان "ع و ن" ثابتًا.

### الإصلاح المطبَّق فعليًا (تم التحقق منه بالفحص المباشر للملفات):

1. لفّ كل حرف بـ `<span data-bind="groomInitial">` و `<span data-bind="brideInitial">` بدلاً من النص الثابت (مع الاحتفاظ بنفس الحرف الافتراضي كـ `data-bind-default` لصفحات المعاينة بدون بيانات).
2. أضفت في بداية دالة `applyData()` بسكربت "VIP Template Data Bridge" (المحقون تلقائيًا في كل قوالب VIP) منطقًا يحسب `groomInitial`/`brideInitial`:
   - يأخذ أول حرف من `groomName` / `brideName` إن وُجدا في بيانات الدعوة.
   - إن لم تتوفر هذه الحقول، يتراجع لتحليل `coupleNames` (تقسيم على "و"/"&"/"and") كحل احتياطي.
   - القيم المحسوبة تُضاف إلى كائن `data` قبل حلقة الربط العامة، فتُطبَّق تلقائيًا على أي عنصر `data-bind="groomInitial"` أو `data-bind="brideInitial"` في الصفحة (بما فيها أكثر من موضع، كما في royal-maroon).

تم تطبيق هذا التعديل بنفس الصيغة على النسختين `vip/` و`vip-demo-files-updated/` حتى تبقيا متطابقتين، وتم فحص توازن الوسوم وصحة صياغة JavaScript بعد التعديل.
