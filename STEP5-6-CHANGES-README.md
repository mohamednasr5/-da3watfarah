# الخطوة 5 و 6 — استكمال خطة توحيد Normal / VIP

## الخطوة 5: Event Schedule + FAQ على قوالب Normal

### المشكلة
`invite.html` هو المحرك الديناميكي الوحيد لكل قوالب Normal (22 تصميم، عبر `TEMPLATES`
+ `applyTheme()` بنفس فكرة `vip-template-renderer.js`)، لكنه كان معندوش أي كود بيقرأ
`eventSchedule` أو `faq` رغم إنهم بيتحفظوا في قاعدة البيانات من الخطوة 3.

### الحل
مكوّن واحد بس، مطبّق مرة واحدة جوا `invite.html`، بيغطي كل قوالب Normal الـ22 من غير
أي تكرار — بنفس منطق `vip/assets/vip-dynamic-sections.js` بالظبط:

- **قسمان جديدان في الـ HTML:**
  - `#scheduleSection` — بعد Gallery وقبل Event Details (نفس الترتيب المطلوب في البرومبت الأصلي).
  - `#faqSection` — بعد Guestbook (`wishesSection`) وقبل الـ CTA/Footer.
  - الاتنين `display:none` افتراضيًا في الـ HTML.

- **دالتان في JS:** `renderScheduleSection(items)` و `renderFaqSection(items)`
  - لو المصفوفة فاضية/مش موجودة → القسم يفضل مخفي بالكامل (`display:none` + تفريغ المحتوى).
  - لو فيها عناصر → يترتّبوا حسب `order` (لو موجود)، ويترسموا بعدد غير محدود من العناصر.
  - بيتنادوا جوا `populateInvitationUI()` (نفس الدالة اللي بترسم كل حاجة تانية)، فبيشتغلوا في:
    - عرض الدعوة النهائي (`invite.html?slug=...` / `?id=...`).
    - المعاينة الحية أثناء الإنشاء (`farah-preview-update` postMessage من `create-invitation.html`).

- **التصميم:** Timeline للـ Schedule (وقت + نقطة + خط + عنوان) و Accordion للـ FAQ
  (رقم + سؤال + سهم + إجابة)، الاتنين مبنيين بـ CSS variables الموجودة أصلاً
  (`--theme-primary`, `--theme-secondary`, `--theme-text`, `--theme-light`) اللي كل قالب
  من الـ22 بيعرّفها في `applyTheme()` — يعني يتلوّنوا تلقائيًا مع أي قالب من غير أي CSS إضافي
  لكل تصميم، بنفس فلسفة `vip-dynamic-sections.js` مع الـ `--ei-*` variables.

- **توافق خلفي:** أي دعوة Normal قديمة من غير `eventSchedule`/`faq` → القسمين بيفضلوا
  مخفيين تلقائيًا، مفيش أي خطأ أو عرض فاضي (بالظبط زي القوالب الـVIP).

### النتيجة
دلوقتي القسمين شغالين بنفس المنطق **بالظبط** على الـ22 قالب Normal + الـ18 قالب VIP —
مكوّن واحد لكل عائلة قوالب، صفر تكرار كود إضافي.

---

## الخطوة 6: توحيد صفحة اختيار القوالب

### الوضع اللي كان موجود بالفعل (اتأكدت منه بالكود)
`create-invitation.html` كانت أصلاً فيها **جريد واحد فقط** (`#templatesGrid`) بترسم بيه
دالة واحدة (`renderTemplates()`) لكل التصاميم — Normal و VIP مع بعض من `TEMPLATES_CONFIG`
واحدة، بنفس الـ card تمامًا:

- نفس الـ Thumbnail / الاسم / الفئة / زر معاينة (`openPreviewModal`) / زر استخدام
  (`selectTemplate`)، ونفس الـ CSS (`.template-card`) لكل التصاميم.
- الفرق الوحيد البصري: `badge-vip` بيظهر بس لو `template.isVip === true` (بالظبط زي
  `badge-premium` و `badge-new`).
- زر المعاينة بيفتح نفس الـ Modal (`previewModal` + `template.previewFile`) لأي تصميم،
  Normal كان أو VIP.

يعني الفرضية الأساسية للخطوة 6 (نفس الجريد، بادج بس) كانت متحققة بالفعل.

### اللي اتضاف
- `loading="lazy"` على صور الـ thumbnails (كان مطلوب صراحة في المواصفات ومش موجود).
- دعم بادج **"مميز" (Featured)** اختياري (`template.isFeatured`) — زي `isPremium`/`isNew`
  بالظبط، بس محدش من الـ38 تصميم مفعّل عليه دلوقتي، فمفيش أي تغيير بصري حالي، لكنه بقى
  جاهز يتفعّل لأي تصميم من غير أي كود إضافي.

### ملاحظة
صفحة `/vip/` (`vip/index.html`) صفحة تانية تمامًا — صفحة تسويقية لعرض الديمو الجاهز
لكل تصميم VIP قبل الشراء (مش صفحة اختيار قالب للفورم)، فمكانتش داخلة في نطاق "صفحة
اختيار القوالب" المقصودة هنا. لو حابب توحيدها هي كمان مع تجربة التصفح العادية، ده تحسين
منفصل ممكن نتكلم فيه.

---

## إصلاح إضافي: القوالب الناقصة من TEMPLATES (10 من أصل 16)

بعد ما خلصت الخطوة 5 و6، لاحظت إن فيه 16 تصميم من ضمن الـ38 المعروضين في نفس الجريد
الموحّد قابلين للاختيار فعليًا من العميل، لكن `invite.html` معندهوش تسجيل ليهم في
`TEMPLATES`، فكانوا بيقعوا على fallback لثيم `preview-classic` (الألوان بس كانت بتتبدّل
عن طريق `applyColorOverride`، لكن التأثيرات والبنية الأصلية للتصميم مكنتش بتتطبّق).

### اللي اتصلح (10 تصاميم)
- `preview-classic-2` إلى `preview-classic-6` (5 تصاميم) — نفس بنية وتأثيرات
  `preview-classic` الأساسي بالظبط، وألوانهم الخاصة بتتطبّق زي ما هي من `TEMPLATES_CONFIG`.
- `preview-romantic-2` إلى `preview-romantic-6` (5 تصاميم) — نفس بنية وتأثيرات
  `preview-romantic` (شامل `hasFloatingHearts`)، وألوانهم الخاصة زي ما هي.
- اتسجّلوا بمرجع للقالب الأساسي (`TEMPLATES['preview-classic-2'] = TEMPLATES['preview-classic']`)
  — صفر تكرار كود، مطابق لمبدأ "لا تكرار" في المواصفات الأصلية.

### اللي لسه ناقص (6 تصاميم) — يحتاج شغل منفصل
تصاميم `templates-new/preview-artistic|elite|floral|minimal|nature|starry` مبنية بنظام
CSS variables مختلف تمامًا (`--primary/--secondary` مش `--theme-primary/--theme-secondary`)
وتصميم مستقل بالكامل عن نظام `style.css`/`invitation.css` المشترك — يعني هي فعليًا زي
تصاميم VIP الساكنة قبل الخطوة 2 بالظبط. تحويلهم لقوالب ديناميكية جوا `invite.html` شغلة
بنفس حجم الخطوة 2 (تحليل كل تصميم + تسجيله بستايلاته وتأثيراته الحقيقية)، مش إصلاح سريع،
فسبتها لمرحلة منفصلة عشان محدش يتفاجئ إنها لسه شغالة بألوان تقريبية بس. قولي لو عايز
أبدأ فيها.

---

## اختبار
- تم فحص الصياغة (syntax) لكل أكواد JavaScript جوا `invite.html` و `create-invitation.html`
  بعد التعديل — سليمة بالكامل.

## الخطوات المتبقية من الخطة الأصلية
لا يوجد — الخطوات الست كلها (1 إلى 6) اكتملت.
