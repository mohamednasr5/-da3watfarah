# الخطوة 4 — بايبلاين عرض موحّد (VIP بس بيضيف Intro قبل الدخول)

## الوضع اللي كان موجود بالفعل (اتأكدت منه بالكود)
`invite.html` أصلاً فيه **مسار واحد فقط** للعرض:
```js
if (VipTemplateRenderer.isVipTemplate(templateFile, data.design)) {
    VipTemplateRenderer.render(data, templateFile, container);   // VIP
} else {
    populateInvitationUI(data);                                   // Normal
}
```
مفيش أي كود مكرر أو قديم بيتعامل مع VIP في مكان تاني في الملف — دورت على كل استخدامات `vip/` في
`invite.html` ومفيش غير الفرع ده. يعني الفرضية الأساسية للخطوة 4 (مسار عرض واحد) كانت متحققة
جزئيًا من الخطوة 2. الناقص كان: **إن الـ Intro يبقى اختياري لكل دعوة**، مش خاصية ثابتة على مستوى
التصميم فقط.

## اللي اتضاف في الخطوة 4

### 1. Toggle "تفعيل شاشة الدخول" — `create-invitation.html`
- في Step 3 (المحتوى والتصميم)، ظهر قسم جديد **يظهر فقط لو القالب المختار VIP** (`isVip: true`):
  checkbox "تفعيل شاشة الدخول الافتتاحية" — مفعّل افتراضيًا.
- لو القالب Normal، القسم مختفي بالكامل (زي ما هو مطلوب: الفرق الوحيد هو الـ Intro، وكل حاجة تانية
  زي بعضها بالظبط).
- بيتحدّث تلقائيًا مع كل اختيار قالب عبر `updateVipIntroToggleVisibility()`.

### 2. حفظ الإعداد — `js/db.js`
- كل دعوة بتتحفظ دلوقتي بحقل `vip: { introEnabled: true|false }` (اتضاف جوا `unified-invitation-schema.js`
  أصلاً من الخطوة 1، ودلوقتي فعليًا بيتملى ويتحفظ).

### 3. تمرير الإعداد للقالب — `js/vip-template-renderer.js`
- الـ payload اللي بيتبعت عبر `postMessage` لكل قالب VIP بقى فيه حقل `introEnabled` جديد
  (افتراضيًا `true` لو مش محدد، عشان القوالب القديمة المحفوظة قبل الخطوة دي تفضل شغالة زي ما هي —
  Backward Compatibility).

### 4. تطبيق الإعداد — `vip/assets/vip-dynamic-sections.js`
- **تطبيق واحد فقط يغطي الـ18 قالب VIP كلهم** (مفيش أي تعديل داخل كل قالب لوحده):
  لو `introEnabled === false`، الكود بيشيل شاشة الدخول (`#einvite-envelope`) فورًا ويفك قفل
  السكرول (`body.env-locked`)، فالدعوة بتفتح مباشرة زي القوالب العادية.
- لو `introEnabled` مش `false` (يعني `true` أو غير موجود)، القالب بيشتغل بسلوكه الأصلي
  (اضغط لدخول القاعة) من غير أي تغيير.
- اتأكدت إن كل الـ18 قالب فيهم نفس بنية الـ HTML (`#einvite-envelope`, `.env-locked`, `.gone`)
  فالتطبيق الواحد ده كافي وشغال على الكل من غير استثناء.

## النتيجة
- **مسار عرض واحد** في `invite.html` (كان موجود، اتأكد منه).
- **الفرق الوحيد** بين Normal و VIP دلوقتي فعليًا قابل للتحكم لكل دعوة: تشغيل/إيقاف الـ Intro.
- **صفر تكرار كود** — منطق تشغيل/إيقاف الـ Intro مكتوب مرة واحدة بس في `vip-dynamic-sections.js`
  ومطبّق على كل القوالب الـ18.
- **توافق خلفي:** أي دعوة قديمة من غير حقل `vip.introEnabled` هتتعامل كأن `introEnabled = true`
  (نفس السلوك القديم بالظبط، من غير أي كسر).

## لسه باقي (الخطوات الجاية)
- **الخطوة 5:** نفس منطق Event Schedule / FAQ (ومنطق الـ Intro لو حبيت VIP-lite لاحقًا) على
  قوالب Normal — دلوقتي القسمين دول بيظهروا بس لو القالب VIP.
- **الخطوة 6:** توحيد شكل صفحة اختيار القوالب.
