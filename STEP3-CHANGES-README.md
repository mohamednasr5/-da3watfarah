# الخطوة 3 — دمج فورم الإنشاء (Event Schedule + FAQ Builder)

هذه الحزمة تحتوي على المشروع كامل بعد دمج الخطوة 1 والخطوة 2 (اللي كانت في `vip-step2-files.zip`)
+ تعديلات الخطوة 3 الجديدة.

## اللي اتعمل في الخطوة 3

### 1. `create-invitation.html`
- **فورم واحد فقط** لكل الأنواع (زي ما كان) — مفيش فورم منفصل لـ VIP، والقوالب اللي فيها `isVip: true`
  بتتعرض جوا نفس `templatesGrid` مع باقي القوالب (ده كان شغال بالفعل من قبل).
- **قسم جديد "برنامج الحفل" (Event Schedule)** جوا Step 3 (المحتوى والتصميم):
  - إضافة/حذف فقرات غير محدودة.
  - سحب وإفلات لإعادة الترتيب (Drag & Drop).
  - ترقيم تلقائي.
  - لو القسم فاضي بالكامل → مفيش أي شيء يترفع، والقسم بيختفي تلقائيًا من صفحة الدعوة.
  - لو المستخدم بدأ يكتب في فقرة ومسيبهاش كاملة (اسم من غير توقيت أو العكس) → تحذير قبل النشر.
- **قسم "الأسئلة الشائعة" (FAQ)** بنفس المنطق بالظبط (سؤال + إجابة، إضافة/حذف/سحب وإفلات، إخفاء تلقائي لو فاضي).
- **دالة `publishInvitation()`** بقت بتبعت `eventSchedule` و `faq` و `isVip` مع باقي بيانات الدعوة،
  بنفس أسماء الحقول اللي بيقرأها `js/vip-template-renderer.js` (اللي جاي من الخطوة 2) —
  يعني القالب هيستقبل البيانات ويعرضها فورًا من غير أي تعديل تاني في القوالب.

### 2. `js/db.js`
- `createInvitation()` بقت بتحفظ `eventSchedule` و `faq` و `isVip` و `type` كحقول على مستوى الدعوة نفسها
  (مش بس جوا `content`)، متوافقة مع `js/unified-invitation-schema.js`.
- `updateInvitation()` متغيرتش — أصلاً بتقبل أي حقول تتبعتلها (generic passthrough)، فهتشتغل صح
  مع الحقول الجديدة من غير أي تعديل.

## اللي لسه شغال زي زمان (Backward Compatibility)
- الدعوات القديمة المحفوظة في قاعدة البيانات من غير `eventSchedule`/`faq` هتفضل شغالة عادي —
  `js/vip-template-renderer.js` بيعمل fallback على مصفوفة فاضية `[]`، والقسم بيختفي تلقائيًا
  (مش هيحصل أي خطأ أو عرض فاضي).

## لسه باقي (الخطوات الجاية)
- **الخطوة 4:** بايبلاين عرض موحّد (VIP بس بيضيف intro قبل الدخول) — لسه.
- **الخطوة 5:** نفس منطق Event Schedule / FAQ لكن على قوالب Normal (`preview-*.html`) —
  دلوقتي الأقسام دي بتترسم بس لو القالب المختار VIP (لأن `vip-dynamic-sections.js` بس اللي بيعمل
  render ليهم). قوالب Normal لسه معندهاش هذا المنطق خالص.
- **الخطوة 6:** توحيد شكل صفحة اختيار القوالب.

## ملاحظة تقنية مهمة
البيانات بتترحّل صح من الفورم لحد الحفظ في قاعدة البيانات، وبتوصل صح للقوالب الـVIP (اتأكدت من
الكود في `vip-template-renderer.js` سطر 166-212 إنه بيقرأ بالظبط `invitationData.eventSchedule`
و `invitationData.faq`). لكن **القوالب Normal (preview-*.html) لسه معندهاش أي كود بيقرأ الحقلين
دول** — فلو نشرت دعوة بقالب Normal وحطيت فيها برنامج حفل أو أسئلة شائعة، هتتحفظ في قاعدة البيانات
صح، لكن مش هتظهر في الصفحة النهائية لحد ما نعمل الخطوة 5.
