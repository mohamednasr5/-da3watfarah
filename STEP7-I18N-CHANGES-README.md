# خطوة 7 + تدقيق الترجمة الكامل (English/Arabic)

هذا الملف بيوثّق التعديلات اللي اتعملت بعد ملف `STEP5-6-CHANGES-README.md`.

---

## 1) الخطوة 7: تحويل آخر 6 تصاميم في `templates-new/` لقوالب ديناميكية

القوالب: **artistic, elite, floral, minimal, nature, starry**

- اتضافوا كـ entries حقيقية جوا `TEMPLATES` في `invite.html` (نفس آلية باقي الـ 32 تصميم قبلهم):
  ألوان/خطوط/حواف/ظلال كل تصميم اتاخدت من الملف الأصلي بتاعه في `templates-new/`.
- **السبب الحقيقي إنهم كانوا بيقعوا على preview-classic**: القيمة المخزّنة فعليًا في
  `design.templateFile` هي `templates-new/preview-artistic` (بالمسار كامل، مش الاسم بس) —
  فاتسجّلوا في `TEMPLATES` بنفس المفتاح ده بالظبط.
- كل تصميم اتضاف له تأثير حركي يناسب شخصيته:
  - **Artistic**: بقع طلاء ملوّنة عايمة (blur blobs)
  - **Elite**: جزيئات ذهبية عايمة لفوق
  - **Floral**: بتلات ورد عايمة
  - **Nature**: ورق شجر ساقط
  - **Starry**: نجوم متلألئة + نجمة ساقطة (shooting star)
  - **Minimal**: خلفية شبكة (grid) بسيطة بدون JS إضافي
- التصاميم التلاتة الغامقة (artistic / elite / starry) اتضاف لها override واضح لألوان
  النصوص في قسمي "برنامج الحفل" و"الأسئلة الشائعة" (الخطوة 5) عشان تفضل واضحة فوق خلفية غامقة.
- الـ Live Preview في `create-invitation.html` بيستخدم نفس `invite.html` جوا iframe، فبيشتغل
  تلقائيًا من غير أي تعديل إضافي.

**الباقي فعليًا خلص من خطة الـ VIP unification كلها.**

---

## 2) تدقيق الترجمة (AR ⇄ EN) — مش مجرد نصوص، لقينا bugs حقيقية

الصفحات اللي فيها زرار تبديل لغة: `invite.html`, `create-invitation.html`, `index.html`, `editor.html`.
(`dashboard.html` مفيهاش زرار لغة، فمش جزء من نطاق التدقيق.)

المنهج: (أ) قارنت كل قاموس ترجمة `ar`/`en` مفتاح بمفتاح للتأكد إنهم متطابقين بالكامل،
(ب) دورت على كل `data-i18n` مستخدم في الصفحة وتأكدت إن ليه مقابل في القاموس،
(ج) عملت scan آلي لأي نص عربي ظاهر في الصفحة *مش* متلفوف بـ `data-i18n` (يعني هيفضل عربي
حتى لو المستخدم بدّل اللغة إنجليزي).

### أهم حاجة اتلقيت: `invite.html` → `EVENT_TYPE_META`
كان فيه object كامل (شارة المناسبة، عنوان الصفحة، اسم الحدث الافتراضي، عنوان العداد
التنازلي، نص الـ RSVP الفرعي) لكل الـ 8 أنواع مناسبات — **عربي فقط، ومش بيسمع لـ switchLang()
خالص.** يعني أي حد يبدّل اللغة إنجليزي، هيفضل شايف "حفل الزفاف" / "العد التنازلي للفرح"
وغيرهم بالعربي زي ما هما بالظبط.

كمان جملة "ذكريات العروسين" فوق المعرض، وجملة عيد الميلاد ("… يكمل/تكمل X سنوات") وجملة
المولود، كانت كلها عربي ثابت مهما كانت اللغة.

**الحل:** اتعمل `EVENT_TYPE_META_I18N` (نسخة ثنائية اللغة لكل الحقول دي)، ودالتين جداد:
`getTypeMeta(eventType, lang)` و`reapplyEventTypeLangText(lang)` — الأخيرة بقت بتتنادى من
جوا `switchLang()` عشان أي دعوة مفتوحة بالفعل تتحدّث فورًا لما حد يبدّل اللغة، من غير ما
يحتاج يعمل reload للصفحة.

### `create-invitation.html`: 23 مفتاح مكتوب في الفورم بس مش موجود في القاموس خالص
دول بالظبط كل حقول **الخطوة 5/6 الجديدة** (برنامج الحفل + الأسئلة الشائعة، تفعيل
الـ VIP intro) + حقول أنواع المناسبات الأحدث (حنة، عيد ميلاد، مولود، تخرج، رمضان). يعني
لما تتضاف ميزة جديدة، مفيش حد ضاف الترجمة بتاعتها. اتضافوا الـ 23 مفتاح بالعربي والإنجليزي.

كمان اتلقيت زرار "صورة عشوائية"، زرار "اختر من الصور/الأغاني المرفوعة سابقًا"، وكل نصوص
مودال "مكتبة الملفات" (العنوان، "جارٍ التحميل..."، "لا توجد ملفات مرفوعة بعد") — عربي ثابت.
اتلفّوا بـ `data-i18n` واتضافت ترجماتهم.

### `index.html`: أكبر عدد فجوات
- 4 كروت مناسبات في الصفحة الرئيسية (حنة/عيد ميلاد/مولود/تخرج) — بالظبط زي أول 3 كروت
  اللي كانت متلفوفة صح، بس دول الأربعة الجداد اتنسوا.
- **17 تصميم VIP** في الجريد: اسم كل تصميم + "الفئة · VIP" تحته + شارة "VIP حصري" — ولا
  واحد فيهم كان متلفوف. اتلفّوا كلهم (الاسم الإنجليزي المستخدم هو نفسه اللي ظاهر أصلاً في
  الـ hover preview بتاعهم، عشان يفضل متسق).
- فلتر أنواع المناسبات فوق الجريد (شريطين مختلفين)، وجملة "قوالب هذا القسم قريبًا... تابعنا".
- اتشال كود JS كان بيدوس على فلتر "الدعوات الحقيقية" بمنطق قديم غير مكتمل (كان بيترجم
  4 بس من أصل 8 أزرار) واستُبدل بـ `data-i18n` عادي على كل زرار.

### `editor.html`
لقيت label واحد ("نشر الدعوة (تظهر للضيوف)") مش متلفوف — اتلف وترجم.

### حاجات اتفحصت وطلعت سليمة (مش bugs)
- أسماء الـ 12 قالب العادي (كلاسيكي/عصري/رومانسي/... إلخ) وشاراتهم ("الأكثر شعبية"،
  "جديد"...) بيتترجموا فعلاً وقت التشغيل عن طريق JS بيقرا من قاموس `T[lang]` — مش
  static HTML، فده سلوك متوقع، مش ثغرة.
- زرار تبديل اللغة نفسه ("العربية") والعلامة التجارية "دعوة فرح" في الفوتر اتسيبوا عمدًا —
  ده معيار شائع (اسم اللغة نفسها، والاسم التجاري، بيفضلوا زي ما هما في أي واجهة).
- رسائل الـ toast/notification المتفرقة (زي "حدث خطأ في جلب الصورة") ما اتلمستش —
  عدد كبير ومنتشر في أكتر من ملف، محتاج مجهود منفصل لو حابب نغطيها كلها.

### التحقق (مش افتراض، اتعمل فعليًا)
- قارنت كل قاموس `ar`/`en` مفتاح بمفتاح (`invite.html`, `create-invitation.html`, `index.html`) —
  0 مفتاح ناقص في أي الاتجاهين دلوقتي.
- دورت على كل `data-i18n` مستخدم في الـ HTML وتأكدت إن مفيش مفتاح واحد من غير ترجمة.
- شغّلت `node --check` على كل `<script>` بلوك في الأربع ملفات المعدّلة — كله سليم.
- عملت HTML parse كامل للأربع ملفات للتأكد إن التعديلات ما كسرتش بنية الصفحة.

---

## خلاصة الحالة الحالية

كل الـ 32 قالب (17 VIP + 6 templates-new + 9 اللي كانوا معمولين قبل كده) بقوا يقرأوا من
نفس الـ schema الموحّد، وبقى فيه فورم واحد ونظام عرض واحد. الترجمة (عربي/إنجليزي) بقت
متسقة بالكامل في الصفحات الأربعة اللي فيها زرار لغة — بما فيها كل حاجة اتضافت في
الخطوتين 5 و6.

**لو حابب خطوة جاية:** تغطية شاملة لرسائل الـ toast/notification (مبعتّرة في أكتر من ملف
JS)، أو مطابقة بصرية أدق للـ 6 تصاميم الجديدة لتفاصيل كل واحد فيهم بالظبط.
